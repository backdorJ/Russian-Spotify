export default class AuthorLittle {
    authorId: string;
    authorName: string;

    constructor(authorId: string, authorName: string) {
        this.authorId = authorId;
        this.authorName = authorName;
    }
}