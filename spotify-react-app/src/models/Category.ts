export class Category {
    categoryNumber: number;
    categoryName: string

    constructor(categoryNumber: number, categoryName: string) {
        this.categoryNumber = categoryNumber;
        this.categoryName = categoryName;
    }
}